name: Deploy to Staging

on:
  push:
    branches: [main]

concurrency:
  group: deploy-staging-${{ github.ref }}
  cancel-in-progress: false

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ${{ github.repository }}

jobs:
  build-and-push:
    name: Build & Push ${{ matrix.component }} image
    runs-on: [self-hosted, mravps]
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - component: api
            context: .
            dockerfile: apps/api/Dockerfile
          - component: web
            context: .
            dockerfile: apps/web/Dockerfile
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract image metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-${{ matrix.component }}
          tags: |
            type=sha,format=long
            type=ref,event=branch
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=${{ matrix.component }}
          cache-to: type=gha,mode=max,scope=${{ matrix.component }}

  # deploy:
  #   name: Deploy to staging cluster
  #   runs-on: [self-hosted, mravps]
  #   needs: build-and-push
  #   environment: staging
  #   steps:
  #     - uses: actions/checkout@v4
  #     # TODO: Wire in kubectl/helm. Helm charts live at infra/k8s/helm.
  #     #   - azure/setup-helm@v4 (or equivalent)
  #     #   - authenticate to cluster (OIDC or kubeconfig secret)
  #     #   - helm upgrade --install opengrade infra/k8s/helm \
  #     #       --namespace staging \
  #     #       --set api.image.tag=sha-${{ github.sha }} \
  #     #       --set web.image.tag=sha-${{ github.sha }} \
  #     #       --atomic --wait --timeout 10m
